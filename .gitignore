# Personal Claude Code config (permissions) — not shared. This pattern ignores the file both
# in this standalone project and while the folder is still nested inside its origin repo.
.claude/settings.local.json

# The shared engine pin (.claude/settings.json) is intentionally NOT ignored — it travels with
# the repo so every clone runs on the same model/effort.

# cycle runtime
/.factory/
/cycle.log
PAUSE

# Python / uv (server)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Node / Vite (reader, admin-ui, shared)
node_modules/
dist/
*.tsbuildinfo

# Fixture-bundle tool scratch output (the committed bundle lives under server/tests/)
/tools/out/

# Runtime data dir (never committed; the repo ships data/styles.json seed only)
/data/library/
/data/work/
/data/sync/
/data/jobs/
/data/users.json
